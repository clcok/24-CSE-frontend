<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:name="com.example.MyApplication"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.ParkingMatePrac"
        tools:targetApi="31">

        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="@string/KAKAO_API_KEY" />

        <!-- RegisterActivity 설정 -->
        <activity android:name=".view.RegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- HomeActivity 설정 -->
        <activity android:name=".HomeActivity" />

        <!-- MainActivity 설정 -->
        <activity android:name=".view.MainActivity" />

        <!-- SearchActivity 설정 -->
        <activity android:name=".view.SearchActivity" />

        <!-- 다른 액티비티 설정 -->
        <activity android:name=".ReserveParkingLotActivity" />
        <activity android:name=".RegisterParkingLotActivity" />
        <activity android:name=".ChargeLogActivity" />
        <activity android:name=".MyPageActivity" />
        <activity android:name=".RegisterPlaceActivity" />

    </application>

</manifest>
